# environments / caches
.venv/
__pycache__/
*.pyc
.pytest_cache/
.env

# dataset (provided for HackMTY 2026 only - never redistribute) and derived tables
audio/
data/
clips/
*.wav
*.zip
manifest.csv
turns/

# large / local artefacts
*.log
share_url.txt
.DS_Store
Thumbs.db
models/exp/

# self-signed TLS material for -Https (contains a private key: never commit)
certs/
