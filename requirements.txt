# Core (fast path: features + trained model + API + dashboard). Python 3.11 - 3.14.
numpy>=2.0
scipy>=1.13
soundfile>=0.12
scikit-learn>=1.5
joblib>=1.4
fastapi>=0.110
uvicorn[standard]>=0.30
python-multipart>=0.0.9
websockets>=12
# Only needed for `run.ps1 -Https` / `HTTPS=1 ./run.sh`: makes the self-signed certificate that lets other
# devices on the wifi open the live call (browsers refuse the microphone on plain http://).
cryptography>=42
httpx>=0.27
pytest>=8

# Optional: semantic layer (transcription + LLM judge). Enable with SEMANTIC_MODE=uncertain|always.
faster-whisper>=1.1
anthropic>=0.40

# Optional: self-supervised embeddings head (ENABLE_EMBEDDINGS=1, training/train.py --embeddings).
# torch>=2.4
# transformers>=4.44
